import React from 'react'
import { render } from 'react-dom'
import './index.html'
import Acquire from './components/Acquire'

const target = document.getElementById('root')
render(<Acquire />, target)
